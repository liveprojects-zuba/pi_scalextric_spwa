angular.module('app').controller('CarControlViewCtrl', CarControlViewCtrl);

CarControlViewCtrl.$inject = [
    '$rootScope',
    'dataService'
];

function CarControlViewCtrl($rootScope,dataService) {
    var vm = this;

   
}